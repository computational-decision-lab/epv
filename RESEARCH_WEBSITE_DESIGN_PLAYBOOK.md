# 审稿人优先的研究网站设计手册

这份手册把“论文项目页”定义为一种证据阅读界面，而不是论文摘要的营销包装。它服务两类读者：

1. 审稿人需要在有限时间内核对问题、方法、证据、边界和可复现性。
2. 非专业读者需要理解一个抽象方法为什么会影响真实世界的高价值决策。

两类读者共享同一条原则：网站必须让人容易区分“论文已经证明的事实”“方法的直觉解释”“可想象的部署场景”和“尚未证明的主张”。美观服从这个区分，交互和图片也不能改变它。

## 0. 工作规则

### 0.1 先锁定四个对象

在写任何页面文案或生成图片前，先填写以下四项：

| 对象 | 必须写清的问题 | 结果 |
| --- | --- | --- |
| 研究对象 | 系统究竟预测、选择、控制或授权什么？ | 一句可复述的定义 |
| 核心命题 | 读者离开页面时必须记住什么？ | 一句不超过 12 个词的命题 |
| 证据对象 | 哪个实验、定理、数据集或审计记录支持命题？ | 可点击的原始来源 |
| 证据边界 | 结果明确没有证明什么？ | 与结果相邻的非主张句 |

### 0.2 每个主张都走三问

在内容审查时，对每个数字、结论和动词逐一追问：

1. 它的权威来源是什么？
2. 它的适用范围和风险对象是什么？
3. 如果读者据此采取行动，什么条件仍然缺失？

若第三问答不出来，主张就不能出现在首屏，也不能用强颜色或大字号强调。

### 0.3 证据强度的视觉顺序

建议使用固定的视觉语义：

- 中性蓝灰：候选、预测、未验证的可能性。
- 青绿色：已经由指定证据支持的状态。
- 琥珀色：需要复核、证据不足或适用边界。
- 砖红色：失败、拒绝、负面结果或不可执行。

颜色不是唯一编码。每种颜色必须同时配有文字、图例、状态词或结构差异。

## 1. 权威事实源地图

页面编辑开始前建立一张“事实源地图”。它防止网站文案脱离论文、把旧实验结果误当成新结果，或把生成图片当成方法定义。

| 优先级 | 事实源 | 可授权的内容 | 不可授权的内容 |
| --- | --- | --- | --- |
| 1 | 定稿论文、补充材料和正式勘误 | 定义、定理、实验设置、正式数字、局限 | 未写入论文的解释性扩展 |
| 2 | 可追溯的源码、配置、锁定环境和运行日志 | 复现实验步骤、版本、输入输出关系 | 仅存在于本地草稿的结果 |
| 3 | 由脚本重新生成的表格、图和指标清单 | 数字、图例、置信界、样本量 | 手工改过但没有生成记录的数字 |
| 4 | 冻结的制品清单、校验和、发布清单 | 哪些文件是公开版本、如何恢复 | 私有数据、凭据、未审计中间文件 |
| 5 | 审稿人指南、阅读笔记、声明登记表 | 阅读路径、主张与来源的映射 | 代替论文的结论 |
| 6 | 网站正文和交互说明 | 简化解释、术语翻译、边界提示 | 新的实验结论或新的性能承诺 |
| 7 | 生成的照片、插画、概念架构图 | 空间直觉、场景氛围、流程隐喻 | 数字、公式、精确算法、实验结果 |
| 8 | 个人草稿、聊天记录、未归档截图 | 仅用于寻找待核对线索 | 任何面向公众的事实 |

### 1.1 冲突处理顺序

当两个来源不一致时，按以下顺序处理：

1. 停止发布相关句子和图片。
2. 找到能生成该结论的脚本、日志或原始表格。
3. 用当前定稿文件重新构建数字和图。
4. 在主张登记表中记录冲突、裁决和校验和。
5. 若无法裁决，网站显示“待核对”或删除该主张，不用较乐观的版本。

### 1.2 主张登记表

最小字段如下：

| claim_id | 页面句子 | 证据文件 | 证据位置 | 适用范围 | 非主张 | 最后核对 |
| --- | --- | --- | --- | --- | --- | --- |
| C-001 | 选中的动作经过条件风险检查后才可执行 | 正式论文 | 方法章节、图表编号 | 注册的流程和风险对象 | 不等于生产安全或法律许可 | 日期和提交者 |

主张登记表是内容的单一事实接口。页面、图注、社交分享文案和演示稿都从它读取，不要各自改写数字。

## 2. 单一核心命题作为视觉主线

### 2.1 命题模板

用下面的句式写出核心命题：

> 一个系统可以提出或选出动作，但只有在针对最终动作的证据满足注册条件后，系统才获得执行权限。

这不是所有论文都要采用的结论，而是一种结构模板。替换其中的研究对象即可：

- “高置信度推荐”不等于“可以自动发单”。
- “通过单元测试的补丁”不等于“可以直接部署”。
- “模型给出的诊断建议”不等于“可以改变护理流程”。

命题必须同时包含一个能力词和一个授权词，例如“预测/执行”“排序/批准”“检测/隔离”。这样读者能看到被研究的缺失层，而不是只看到一个抽象的安全口号。

### 2.2 设计一张签名图

全站只保留一张需要被记住的主图。推荐结构：

~~~
候选动作
    ↓
资格与来源检查
    ↓
冻结的选择器
    ↓
最终动作的证据检查
    ↓
执行 / 复核 / 拒绝
~~~

主图的每个状态都必须在旁边有语义 HTML。图片负责空间关系，文字负责精确含义。不要把箭头、状态词或数字只画在位图里。

### 2.3 首屏的三层信息

首屏只回答三件事：

1. 研究对象是什么。
2. 核心命题是什么。
3. 当前证据已经覆盖到哪里。

推荐结构：

- 小型研究元信息行：研究类型、版本、主要制品入口。
- 低于六行的核心命题标题。
- 一句面向非专业读者的翻译。
- 证据范围与不覆盖范围并列。
- 两到三个直接入口：论文、方法、代码或制品。
- 右侧签名图，或在窄屏改为紧随标题的纵向流程。

首屏不要放完整摘要、长作者列表、孤立大数字、没有来源的“领先”形容词或自动播放视频。第一屏底部应露出下一段内容的一部分，让读者知道页面还有证据路径。

## 3. 首页叙事顺序

下面的顺序兼顾审稿人的核查路径和非专业读者的理解成本。可以合并章节，但不要打乱“问题、方法、证据、边界”的因果关系。

| 顺序 | 页面段落 | 读者要回答的问题 | 推荐载体 |
| --- | --- | --- | --- |
| 1 | Hero | 这篇研究在防止什么错误？ | 核心命题、签名图、范围声明 |
| 2 | Evidence scope | 哪些结果已经成立？ | 三项关键证据和来源链接 |
| 3 | Missing layer | 为什么预测或选择还不够？ | 现实问题、失败模式、对象表 |
| 4 | Method | 方法在什么顺序上增加了什么检查？ | 语义架构图、精确论文图 |
| 5 | One interaction | 阈值或支持条件变化会怎样？ | 单一决策门实验 |
| 6 | Practice cases | 这个问题如何出现在高价值工作流？ | 四个场景模板和边界标签 |
| 7 | Formal and empirical evidence | 定理、协议和实验各支持什么？ | 结果表、图、证据台账 |
| 8 | Negative evidence | 方法在哪些地方没有转移？ | 负面结果、失败案例、开放问题 |
| 9 | Limits | 哪些条件不满足就必须暂停？ | 适用范围、拒绝条件、风险清单 |
| 10 | Artifacts | 如何检查和复现？ | PDF、源码、配置、校验和、指南 |
| 11 | Reader | 读者下一步如何核对原文？ | 内嵌阅读器和直接下载 |
| 12 | Citation | 如何准确引用？ | 单一引用块和版本信息 |

“Evidence scope”应出现在摘要之前。它不是营销指标条，而是把结果、样本、风险对象和非主张放在同一个视野内。

## 4. 面向非专业读者的高价值实践案例模板

### 4.1 通用模板

每个案例使用同一套字段，避免不同领域的文案暗中提高或降低证据标准：

1. **决策问题**：用一个真实动作的问句开头，例如“是否应该合并并部署这次变更？”
2. **候选来源**：列出可能提出建议的模型、规则、工具、检索结果或人工角色。
3. **后果动作**：说明动作如何改变外部状态，而不是只说“给出建议”。
4. **可能失败**：描述选择偏差、数据缺失、提示注入、相关失败、制度约束或不可逆后果。
5. **执行前证据**：写出冻结的流程、来源、评估、局部支持、可逆性和责任角色。
6. **路由**：明确什么情况授权、什么情况复核、什么情况拒绝。
7. **边界**：指出案例是部署直觉还是论文实验，并列出不能外推的结论。

每个案例的顶部和图片图注都使用持久标签：

> Deployment scenario, not an empirical result of this study.

中文版可写为：

> 部署场景示意，不是本研究的实证结果。

标签不能只放在鼠标悬停、折叠面板或图片的替代文本中。它必须出现在可见正文，并且可被屏幕阅读器读取。

### 4.2 四类高价值场景

| 场景 | 后果动作 | 典型失败 | 执行前证据 | 必须复核的边界 | 不可暗示 |
| --- | --- | --- | --- | --- | --- |
| AI coding | 合并并部署数据库迁移、权限改动或安全补丁 | 浅层测试通过但丢数据、扩大权限、执行了仓库中的注入指令 | 冻结工具链和选择器、受保护测试、差异来源、回滚验证、任务外不良变更留出验证 | 删除模式、凭据变更、生产发布、支持不足 | 自动部署安全或代码质量保证 |
| 金融 | 转账、交易、授信或组合再平衡 | 排序放大信心，却丢失限额、流动性、对手方、欺诈或制度约束 | 注册损失事件、敞口分组、选择器、限额、校准窗口、双重控制、审计记录 | 高额转账、新对手方、制度例外、市场状态变化 | 投资适当性、合规许可或无损失保证 |
| 医疗工作流 | 改变分诊优先级、发送医嘱或改变照护流程 | 选择压低不确定性、忽略亚组或把建议变成无支持的指令 | 验证过的工作流、亚组评估、来源、校准的不良事件代理、负责的专业人员 | 诊疗授权、脆弱亚组、上下文缺失、证据冲突 | 临床疗效、安全或替代专业判断 |
| 网络安全 | 隔离主机、撤销凭据、封禁账户或轮换密钥 | 提示注入、污染遥测、相关代理失败导致破坏性响应 | 可信来源、对抗评估、爆炸半径、可逆路径、冻结规则、事件类别支持 | 组织级锁定、身份根变更、破坏性修复、分布外事件 | 运营环境安全认证或自动响应无风险 |

### 4.3 面向普通读者的写法

先写动作，再写缺失的证据：

> 一个助手可能已经找到看起来合理的补丁。问题不是它能不能提出补丁，而是系统是否检查了最终补丁的来源、回滚路径和生产影响，才决定是否允许部署。

避免以下句式：

- “该方法保证金融决策安全。”
- “该架构可以自动化医疗授权。”
- “该图展示了系统在真实企业中的效果。”

替换为：

- “这个场景说明为什么执行权限需要独立证据。”
- “在真实部署中，这些条件仍需要领域验证和责任人批准。”
- “示意图帮助读者理解流程，不新增实验结论。”

## 5. 证据边界与非主张系统

### 5.1 四种内容类型

在数据模型或 HTML 中区分四种类型：

| 类型 | 视觉标记 | 允许的语言 | 示例 |
| --- | --- | --- | --- |
| 实证结果 | 结果色、来源链接、样本信息 | “在注册任务上观察到” | 某风险代理的估计值和界 |
| 形式分析 | 定理或命题标记 | “在假设 A、B 下推导” | 选择对象的风险表达式 |
| 概念示意 | 中性色和“概念”标签 | “帮助理解”“说明可能的工作流” | 生成的架构图 |
| 非主张/负面证据 | 琥珀或砖红、明确否定 | “没有证明”“外部序列未确认” | 迁移失败或支持不足 |

同一数字不得同时出现在“实证结果”和“概念示意”组件中。概念交互可以使用合成数值，但标题、图注和可访问名称必须说明“示例计算，不是论文结果”。

### 5.2 边界标签的放置规则

- 场景标题上方放一次。
- 场景图片图注再放一次，防止图片被单独分享时失去上下文。
- 交互组件的说明、状态更新和下载文案保持同一措辞。
- 不使用只有颜色能识别的“安全/危险”状态。
- 负面结果必须与正面结果使用相同字号、同等可见度和可点击来源。

## 6. 图像生成与语义 HTML/SVG 的分工

### 6.1 选择载体

| 内容 | 首选载体 | 原因 |
| --- | --- | --- |
| 方法步骤、状态词、公式、数值、坐标轴 | 语义 HTML 或 SVG | 可搜索、可复制、可访问、可验证 |
| 论文中的精确实验图 | 源脚本导出的 SVG/PNG/PDF | 与数字和论文保持一致 |
| 领域场景、空间隐喻、概念氛围 | 生成的位图 | 提供直觉，不冒充证据 |
| 可交互阈值和路由 | HTML、CSS、少量 JavaScript | 状态可读、可键盘操作 |
| 装饰性背景纹理 | 尽量不用；必要时使用压缩位图 | 不让装饰抢夺证据层级 |

生成位图不得承载可读标题、数字、公式、图例、流程状态或实验结论。若模型在图片中生成了文字、徽标、伪造结果或不准确的专业器械，应拒绝该版本，重新生成或使用图形代码重画。

### 6.2 替代文本规则

- 有信息价值的场景图：描述场景、动作和证据边界，不描述无法核实的模型“性能”。
- 纯装饰或完全由旁边语义列表解释的架构图：使用空替代文本，并把完整含义放进有序列表或正文。
- 精确论文图：替代文本说明图展示的变量、比较和范围，图注说明结果语义。
- 不把“generated image”作为唯一替代文本；它没有告诉读者图片表达什么。

### 6.3 生成提示词模板

英文提示词可采用以下结构，具体领域词由研究者替换：

~~~
Create a high-resolution editorial research illustration of [consequential action].
Show multiple candidate signals converging on a decision gate, with visible
evidence review, a reversible path, and a human accountability boundary.
Use a restrained blue, teal, amber, and brick-red palette on a light neutral
background. No readable text, numbers, logos, brand marks, or claims of
measured performance. The image is a conceptual deployment scenario, not an
empirical result.
~~~

提示词本身也要保存到私有生成记录，记录模型、尺寸、质量参数、时间、输入参考图和最终文件校验和。公开仓库只需要发布经过审查的结果和简短的来源说明，不要发布凭据。

## 7. 架构图工作流

### 7.1 两层架构

一张可靠的架构图由两层组成：

1. **空间层**：生成位图或手工绘制的几何关系，让读者一眼看到候选、门控和路由。
2. **语义层**：HTML/SVG 的文本、编号、状态、图例和来源，让读者能读、搜、验证和操作。

空间层不能替代语义层。精确算法流程还要紧邻论文原图，避免概念图被误认为正式方法图。

### 7.2 十步流程

1. 从论文和主张登记表提取节点、边、状态和不变量。
2. 写一份不含视觉风格的语义草图，逐项标出哪些词必须可读。
3. 将“候选、选择、证据、授权、复核、拒绝”映射到稳定的颜色和形状。
4. 生成最高质量的本地母版，建议保留 3840 x 2160 的 16:9 文件。
5. 检查图片是否含有可读文字、数字、伪造图表、错误器械或暗示性品牌。
6. 对不合格版本进行编辑或废弃，不在 CSS 中用裁剪隐藏错误。
7. 从母版导出网页版本，通常为 1600 x 900 的 WebP 或 AVIF；保留母版在私有制品目录。
8. 在 HTML/SVG 中补齐完整标签、列表、图注、替代文本和证据边界。
9. 将概念图放在方法解释前，把精确论文图放在概念图后。
10. 在真实浏览器中滚动触发懒加载，检查像素、尺寸、MIME、键盘和移动端布局。

### 7.3 文件布局建议

~~~
private-artifacts/
  image-masters/           # 4K 母版、提示词、生成日志，不公开
  checksums.txt
public-site/
  assets/visuals/          # 优化后的网页版本
  assets/figures/          # 论文源脚本导出的精确图
  index.html
  styles.css
  app.js
~~~

母版不进入公开仓库，也不因为“图片已经生成”就绕过发布边界检查。

## 8. 视觉 Tokens、排版与减少卡片

### 8.1 最小 Token 集

Token 名称应表达语义角色，而不是具体组件：

| Token | 作用 | 约束 |
| --- | --- | --- |
| canvas | 页面背景 | 中性、接近白色，不使用大面积渐变 |
| ink | 正文和主标题 | 小字对背景至少 4.5:1 |
| muted | 次要说明 | 仍需达到正文对比度，不能用“浅灰美学”牺牲阅读 |
| structural | 结构线和链接 | 与背景有清晰边界 |
| evidence | 已验证状态 | 同时配文字或图标 |
| boundary | 复核和非主张 | 为小字单独准备更深的 boundary-ink |
| adverse | 失败和拒绝 | 不作为装饰性主色 |

推荐使用 OKLCH 或等价的语义色系统。每次改色都用实际渲染后的前景和背景计算对比度，不能只看色板数值。

### 8.2 排版

- 标题可用有个性的衬线字体，正文使用高可读无衬线字体，代码和短标签才使用等宽字体。
- 建立 1.25 以上的字号层级；正文保持 65--75 个字符的行宽。
- 标题使用平衡换行，长段落使用自然换行；不要用负字距挤压单词。
- 通过断点调整字号，不依赖视口宽度连续缩放到难以预测。
- 公式、表格数字和置信界使用等宽或 tabular numbers，防止列跳动。
- 不依赖远程字体作为首屏必要资源。

### 8.3 布局与卡片

优先使用完整色带、细分隔线、留白、网格和图文关系。卡片只在以下情况使用：

- 一组真正重复且需要逐项比较的证据条目。
- 一个边界清晰的交互工具。
- 下载、引用或制品选择。

不要把整页拆成相同尺寸的圆角卡片，也不要在卡片里面继续嵌卡片。一个页面最多保留一个主交互区域，其他内容用叙事层级和规则线组织。

## 9. 唯一高价值交互：Decision Gate Playground

交互的目标不是把论文做成仪表盘，而是让读者观察一个明确的因果关系：

> 提高阈值可能减少授权数量，但没有局部证据的动作仍应进入复核或拒绝。

### 9.1 交互契约

- 只展示一个小型、可解释的候选集合。
- 控件可调整阈值、执行成本或支持条件中的一到两个变量。
- 结果同时显示授权、复核、拒绝的数量和理由。
- 输出区使用 aria-live，并以文字说明状态改变。
- 标题和图注持续显示“概念示例，不是论文结果”。
- 默认值来自公开的示例配置，不冒充实验数字。
- 不加入账户、筛选器、排行榜、实时市场数据或长时间动画。

### 9.2 键盘和状态

优先使用原生 input、button、a、table、ol 和 dl。自定义 tab 必须实现：

- 选中状态与 aria-selected 同步。
- aria-controls 指向唯一面板。
- 方向键、Home、End 和 Tab 顺序符合预期。
- 面板有 aria-labelledby，隐藏面板不进入焦点顺序。
- 颜色变化之外还有文字状态。

动画只用于短暂的状态过渡，并为 prefers-reduced-motion 提供静态版本。

## 10. 移动端、无障碍和性能

### 10.1 移动端验收

- 在 320、360、390、768 和桌面宽度测试，根文档不能出现意外横向滚动。
- 通过锚点跳转时，粘性页眉不能遮挡标题或边界标签。
- 表格放在有说明的横向滚动容器中，正文和关键结论不要求横向滚动。
- 16:9 图片保留稳定宽高比，避免懒加载时页面跳动。
- 长标题、长单词和按钮文案必须在窄屏换行而非溢出。
- 200% 文本放大后仍能按顺序阅读和操作。

### 10.2 无障碍

- 只有一个 h1，标题层级不跳级，页面拥有 header、nav、main、footer 等地标。
- 每个有意义的图片有准确替代文本；概念图的完整语义不能只存在图片里。
- 表格使用 caption、thead、th 和适当的 scope；公式旁边提供普通语言解释。
- 所有控件有可见名称、焦点样式和键盘路径；不要用 div 冒充按钮。
- 文本对比度达到 WCAG AA；边界颜色需用专门的深色文字 Token。
- 动态结果用状态文本或 aria-live 传达；错误不能只用颜色表示。
- 内嵌 PDF 有 title，且始终提供直接下载链接。

### 10.3 性能

- 4K 母版只留在私有制品目录；网页发布优化后的 WebP/AVIF。
- 为图片写明确的 width、height、loading 和 decoding 属性；首屏主图才考虑高优先级。
- 首屏以下的图和 PDF 阅读器按需加载，避免初次访问下载整个制品包。
- 不引入不必要的前端框架和远程字体；内容在 JavaScript 失效时仍可阅读。
- 动画只改变 transform 和 opacity，不对大面积模糊、布局尺寸或阴影做连续动画。
- 使用浏览器性能面板检查 LCP、INP、CLS；目标是至少达到当前 Web Vitals 的“良好”区间。

## 11. 公开与私有发布边界

### 11.1 公开快照允许清单

- 已审计的 index、样式、脚本和网页图片。
- 论文、补充材料、阅读指南和复现说明。
- 可公开的源码、配置模板、运行命令和校验和。
- 不包含敏感信息的结果表和精确论文图。

### 11.2 私有内容禁止进入公开快照

- API 密钥、访问令牌、私钥、环境文件和本地凭据。
- 未脱敏数据、受限数据、原始供应商输出和内部日志。
- 4K 图片母版、生成提示词中的秘密上下文和未审查的编辑版本。
- 私有研究树、临时目录、测试缓存、构建日志和无关实验。
- 任何会暴露内部路径、旧仓库标识或未发布结论的文件。

### 11.3 发布原则

1. 在独立临时目录构建公开快照，不从脏的研究树直接推送。
2. 使用显式允许清单复制文件，禁止把整个工作目录作为发布源。
3. 对 HTML、脚本、样式、文档和文件名做敏感词、凭据和旧标识扫描。
4. 为公开快照生成 manifest 和 SHA-256；原始文件只在确认远端校验一致后清理。
5. 公开站点与私有研究仓库使用不同的远端和权限，身份命名保持一致且不泄露内部后缀。

若凭据曾出现在聊天、终端历史或截图中，应立即轮换。不要把“已从代码删除”当作凭据已经安全。

## 12. 测试、截图、MIME 与线上验证

### 12.1 静态检查

- 检查 HTML 是否有唯一 id、正确的 aria-controls/aria-labelledby 和完整 alt。
- 检查每个案例都有边界标签、四个字段和非主张说明。
- 检查所有图片路径存在，网页版本尺寸与声明一致。
- 检查文本、文件名、脚本和公开文档不包含敏感词、旧标识或凭据模式。
- 检查 Markdown、CSS、JavaScript 没有语法错误和尾随空白。

### 12.2 浏览器行为

使用真实浏览器而不是只用静态字符串：

- 点击每个实践 tab，使用方向键、Home、End，并确认选中面板和焦点同步。
- 调整交互阈值，确认路线、理由和边界标签同时更新。
- 让每个懒加载图片滚动进入视口，等待 naturalWidth 大于零并调用 decode。
- 在 320、390、768、1440 宽度检查 scrollWidth、主要图块尺寸和文字溢出。
- 记录 console error、pageerror、requestfailed；PDF 内核的已知中止要与站点错误区分。

### 12.3 截图策略

懒加载页面不能直接用一次 full-page 截图作为证据，因为未进入视口的图片可能仍是空白占位。推荐流程：

1. 首屏截图。
2. 逐段滚动到架构图、每个案例、结果表和 PDF 阅读器。
3. 等待图片解码后保存局部截图。
4. 再保存全页截图，并标记哪些资源已预加载。
5. 对桌面、窄屏和中等宽度做视觉回归比较，重点检查标题换行、表格和粘性页眉。

### 12.4 MIME 与线上资源

每个公开资源至少核对一次：

~~~
curl -I https://PUBLIC_SITE/
curl -I https://PUBLIC_SITE/assets/visuals/method.webp
curl -I https://PUBLIC_SITE/assets/paper/main.pdf
~~~

期望结果：

- HTML 是 text/html。
- WebP/AVIF 是对应的 image MIME，而不是 application/octet-stream。
- PDF 是 application/pdf。
- 所有 URL 返回 200，文件大小非零，远端 SHA-256 与 manifest 一致。
- 页面引用的远端仓库、站点路径和当前发布提交一致。

## 13. 可复制的执行阶段

| 阶段 | 工作内容 | 进入条件 | 退出条件 |
| --- | --- | --- | --- |
| A. 事实锁定 | 建立事实源地图和主张登记表 | 论文和制品可读 | 每个主张都有来源和边界 |
| B. 叙事设计 | 写核心命题、首屏和阅读路径 | 研究对象已定义 | 普通读者能复述问题，审稿人能找到证据 |
| C. 视觉系统 | 定义 tokens、字体、间距、图表语义 | 命题已批准 | 颜色、字号和状态规则机器可读 |
| D. 语义实现 | 先写静态 HTML、表格、图注和下载入口 | 视觉结构稳定 | 无脚本时内容仍完整 |
| E. 交互实现 | 只实现一个高价值决策门 | 状态和证据边界已写 | 键盘、屏幕阅读器和降级路径可用 |
| F. 图片生产 | 生成 4K 母版、审查、导出网页版本 | 提示词不含秘密且无文字要求 | 母版、优化图、alt、图注和校验和齐全 |
| G. 质量验收 | 跑静态、浏览器、视觉和性能检查 | 公开快照已构建 | 所有高优先级问题关闭 |
| H. 发布验证 | 推送独立公开快照并核对线上资源 | 允许清单和 manifest 完成 | 远端工作流成功，线上 MIME 和内容一致 |

不要跳过 F 直接用生成图片替代精确论文图，也不要跳过 H 就把本地“能打开”称为发布完成。

## 14. 发布前验收清单

### 事实与叙事

- [ ] 首屏有一个可复述的核心命题。
- [ ] 首屏同时显示证据范围和不覆盖范围。
- [ ] 结果、定理、概念示意和负面证据使用不同内容类型。
- [ ] 每个数字能回到正式来源和生成脚本。
- [ ] 高价值案例都明确写出执行动作、失败模式、执行前证据和复核边界。
- [ ] 案例和交互不会暗示生产安全、临床疗效、投资适当性或运营认证。

### 视觉与内容

- [ ] 签名图只表达一个核心关系，精确方法图紧邻其后。
- [ ] 生成位图没有可读文字、数字、徽标或伪造结果。
- [ ] 所有图都有合适的 alt、尺寸、图注和来源边界。
- [ ] 页面不用紫蓝渐变、重复卡片矩阵、装饰性玻璃效果或无意义的滚动动画。
- [ ] 负面结果和局限性与正面结果同等可见。

### 交互与无障碍

- [ ] 原生控件可用，所有交互都有可见焦点和键盘路径。
- [ ] 自定义 tab 的选中状态、面板可见性和焦点完全同步。
- [ ] 动态输出有文本状态和 aria-live，颜色不是唯一线索。
- [ ] 320px 和 200% 放大不产生根级横向溢出。
- [ ] 文本和小型状态标签达到 WCAG AA 对比度。
- [ ] PDF 有标题、直接下载链接和失败时的替代路径。

### 性能与资源

- [ ] 4K 母版未进入公开快照，网页图片经过压缩且有稳定尺寸。
- [ ] 首屏以下资源懒加载或按需加载，首屏不依赖远程字体。
- [ ] 控制台没有未解释的错误，图片能在真实浏览器中解码。
- [ ] WebP/AVIF、PNG、PDF 的线上 MIME 正确，资源返回 200。
- [ ] 页面在桌面、平板和手机截图中没有重叠、裁切或空白占位。

### 发布边界

- [ ] 公开快照由允许清单生成，未复制私有研究树。
- [ ] 敏感词、凭据模式、内部路径和旧标识扫描通过。
- [ ] manifest、校验和、公开提交和线上提交一致。
- [ ] 线上首页、图片、论文和指南都能直接打开。
- [ ] 发布工作流成功，失败时不宣称“已上线”。

## 15. 可直接复制的页面骨架

~~~
Hero:        核心命题 + 证据范围 + 签名图
Evidence:    三项结果 + 来源 + 非主张
Problem:     一个失败动作 + 选择为何改变风险对象
Method:      语义架构图 + 精确论文图 + 方法不变量
Playground:  一个阈值或支持条件交互，明确是概念示例
Practice:    AI coding / 金融 / 医疗 / 网络安全四个同构案例
Results:     协议、表格、置信界、负面迁移和开放问题
Limits:      适用条件、拒绝条件、责任边界
Artifacts:   论文、补充、代码、配置、manifest、复现指南
Reader:      内嵌阅读器 + 直接下载
Citation:    版本化引用
~~~

这份骨架不是模板皮肤。每个项目都应先替换事实源、风险对象和边界，再决定颜色、图片和交互。若页面无法在五分钟内让审稿人找到“主张、来源、限制”，应回到第 1 节重新锁定事实。
